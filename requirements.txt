# ============================================================================
# LangPy Minimal Requirements
# ============================================================================
# This is a lightweight version with only essential dependencies.
# Install time: ~2-3 minutes (vs 30+ minutes for full requirements.txt)
#
# Install: pip install -r requirements-minimal.txt
# ============================================================================

# Core Framework
pydantic>=2.0,<3.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
typing-extensions>=4.11.0

# LLM Providers (add only what you use)
openai>=1.0.0              # OpenAI GPT models
anthropic>=0.7.0           # Claude models (optional)
# mistralai>=0.0.10        # Mistral models (optional)

# HTTP Clients
httpx>=0.24.0
aiohttp>=3.8.0
requests>=2.31.0

# Async File Operations
aiofiles>=23.0.0

# Data Validation & Serialization
jsonschema>=4.0.0
PyYAML>=6.0.0

# Document Parsing (Basic)
PyPDF2>=3.0.0              # PDF parsing
python-docx>=0.8.11        # Word documents
beautifulsoup4>=4.12.0     # HTML parsing
markdown>=3.4.0            # Markdown parsing

# Vector Storage (Lightweight)
faiss-cpu>=1.7.4           # Fast vector similarity search
numpy>=1.24.0,<2.0         # Required by FAISS

# Database (Optional - only if using pgvector)
# pgvector>=0.2.0
# psycopg2-binary>=2.9.0
# sqlalchemy>=2.0.0

# Text Processing
# tiktoken>=0.5.0          # OpenAI token counting (optional)

# Development/Testing (Optional)
# pytest>=8.2.0
# pytest-asyncio>=0.23.0
